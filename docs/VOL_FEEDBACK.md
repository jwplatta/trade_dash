# Vol Tab Feedback

- [x] default the start date to like the first of the current month
- [x] Move the parameter controls to left side like we did on the gamma map Tab
- [x] So when the frequency is 1min, 5min, 30min we need to remove the gaps between trading sessions or make it so there's just a small gap. So we probably datetimes along the x-axis when a min frequency is selected. For the day frequency, then we use dates. For both we should filter out weekend days as these sections can take up a large part of the graph and show nothing important.